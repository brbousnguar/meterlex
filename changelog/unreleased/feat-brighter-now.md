### Changed

- The reading over time is no longer six grey bars: each bar takes the colour of the harness that did most of that bucket's work, and the busiest one stays ink. Machine cards do the same with the harness that machine mostly runs. Closes #18.
- The Now screen uses the width of a desktop: the folder map sits beside the harness and machine readings, on a 1200px measure.
- Sub-lines and section notes moved from the lightest ink to `--ink-2`, figures went up a step, and the token split bar starts deeper in the ramp, so the screen stops reading as faded.
- A machine card's chart prints its readout above the bars instead of floating over them, and no longer claims "0 replies" for a series that does not carry reply counts.
