### Fixed

- A reply counted toward another repository than its working folder's no longer carries the working folder's branch (`~/Server` listed `chore/ports-minerva`).
- `run --reattribute` now clears a stored branch that no longer applies, instead of keeping it.
